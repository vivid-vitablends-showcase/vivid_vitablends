{
  "name": "vivid-vitablends",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "prepare": "node -e \"process.env.CI && process.exit(0)\" || husky",
    "lint-staged": "lint-staged"
  },
  "devDependencies": {
    "husky": "^9.0.11",
    "lint-staged": "^15.2.0"
  },
  "dependencies": {
    "minimatch": "^10.2.3"
  }
}
